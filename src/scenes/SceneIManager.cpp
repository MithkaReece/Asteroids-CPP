
#include "scenes/SceneIManager.hpp"
// TODO: Doesn't work, no idea why. Just says no to linkers.
/*
namespace Scene
{
  IManager::IManager(entt::registry &registry, sf::RenderWindow &window)
      : registryRef(registry), windowRef(window) {}
}*/